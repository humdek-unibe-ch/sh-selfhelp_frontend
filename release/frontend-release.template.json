{
  "$comment": "Channel/build metadata only. The compatibility ranges live in /release-manifest.json (single source for the registry resolver AND the descriptor step).",
  "kind": "selfhelp-frontend-release",
  "channel": "stable",
  "builtFrom": {
    "nextStandalone": true
  }
}
